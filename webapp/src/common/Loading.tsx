import * as React from 'react';

export class Loading extends React.Component {
    render(): JSX.Element {
        return (
            <div>
                Loading
            </div>
        );
    }
}
